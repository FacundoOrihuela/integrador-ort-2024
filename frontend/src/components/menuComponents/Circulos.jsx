import { Box, Typography } from "@mui/material";

const Circulos = () => {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4">Círculos de crecimiento</Typography>
      <Typography>Contenido de los Círculos de crecimiento...</Typography>
    </Box>
  );
};

export default Circulos;