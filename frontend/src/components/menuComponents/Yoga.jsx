import { Box, Typography } from "@mui/material";

const Yoga = () => {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4">Bienestar corporal</Typography>
      <Typography>Contenido de Bienestar corporal...</Typography>
    </Box>
  );
};

export default Yoga;